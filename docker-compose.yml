services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: psh_user
      POSTGRES_PASSWORD: psh_password
      POSTGRES_DB: psh_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: